// Central frontend config — use Vite env var VITE_API_URL to configure API base.
// export const API_BASE = import.meta.env.VITE_API_URL || 'https://nuve-be.vercel.app'
export const API_BASE = import.meta.env.VITE_API_URL || 'http://localhost:3030'

// Usage: import { API_BASE } from '../config.js' and then fetch(`${API_BASE}/api/products`)
